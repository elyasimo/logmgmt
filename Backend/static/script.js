// Client-side JavaScript for the Log Management API
document.addEventListener('DOMContentLoaded', (event) => {
    console.log('Log Management API frontend loaded');
});

function showNotification(message) {
    alert(message);
}

